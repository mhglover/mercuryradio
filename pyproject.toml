[project]
name = "mercuryradio"
version = "0.1.0"
description = "A private Discord where friends hear the same stream and rate the song playing now."
requires-python = ">=3.11"
dependencies = [
    "discord.py[voice]>=2.4",
    "python-dotenv>=1.0",
    "mutagen>=1.47",
]
